{
  admin off
}

# Domain is provided via env var DOMAIN.
# Example:
#   DOMAIN=game.example.com
{$DOMAIN} {
  encode zstd gzip

  reverse_proxy game:6868

  header {
    X-Content-Type-Options nosniff
    Referrer-Policy no-referrer
  }
}
